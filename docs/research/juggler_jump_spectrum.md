# Juggler: the jumps of the meander prefactor

Generated by `python -m research.juggler_sequence.jump_spectrum`.

## Answer

`a_n = a_1 * N_n / (2*theta)^(n-1)`, with `N_n` the survivor counts and `theta = 0.9659065532`.

- exact survivor counts `N_1..9 = [1, 1, 2, 3, 4, 8, 13, 19, 38]`
- one measured constant, `a_1 = 0.42623` +/- `0.0002`
- Sturmian zeros: `184` of them, ratio `1.0352968376` to `0.0e+00`
- barrier-index classes agree exactly: `0.0e+00` over `200` classes
- `C_j` available to `j = 2523`, against about `25` for the fits
- `G(1) = 6.72165` truncated, tail about `0.3447`, ledger `7.07`
- total variation of `psi` (downward jumps) `4.7112`

## Why the `1/rho` law is trivial

rho and theta are the same number and a Sturmian zero is an even barrier letter, which kills nothing and doubles the count; the law is the empty-window theorem and nothing more.

## What this does not say

This determines the jumps of psi, not psi. A model of psi as its jumps plus the linear rise periodicity demands leaves a residual of 1.35e-02 against a noise floor of 4.4e-03, and 89 percent of that residual variance is a reproducible function of the coordinate. So psi has structure beyond its jump spectrum, and this branch does not say what it is. Nor is any of this a proof: the asymptotic psi itself remains measured, and the Lean module proves only the finite exchange cost that the spectrum rests on.
