# Paper A's finance, transposed to Collatz, reproduces Eliahou 1993 and Hercher 2018

Generated by `python -m research.juggler_sequence.collatz_finance_mirror`.

## The mirror

- `K log 2 - floor(K x) log 3 = log 3 * frac(K x)`
- `ceil(L x) log 3 - L log 2 = log 3 * (1 - frac(L x))`
- `Lambda_J(L) = log 3 - Lambda_C(L)` (residual `0.0e+00`)
- cycle-level dictionary: `x_min <-> n log n (not the walk-level log x <-> log log n)`

## Collatz side, `x_min <= p / (3 Lambda_C)`

- floor `2^40`: smallest survivor `17087915`; all `13869` survivors below `400000000` in Eliahou's lattice = `True`; brute force agrees below `30000000` = `True`
- floor `695 * 2^60`: smallest `114208327604` with `72057431991` odd steps (Hercher 2018)
- floor `2^68`: smallest `114208327604` with `72057431991` odd steps
- floor `2^71`: first two `[114208327604, 217976794617]` (Barina 2025 is the second)

## Juggler side, `n log n <= L / (1 - exp(-Lambda_J))` (Theorem 4.4, constant 1)

- floor `10^6`: smallest `1054`, first `[1054, 2108, 3162, 4216, 5270, 6324, 7378, 8432, 9486, 10540, 11594, 12648]`
- floor `3.5e8`: smallest `50508`, first `[50508, 101016, 151524, 176251, 202032, 226759, 252540, 277267, 303048, 327775, 352502, 353556]`
- the 1-cycle length `9809721694` has bound `1.025e+20` against the floor `6.886e+09`: not excluded by finance

## What this does not say

No cycle of any length is excluded or claimed in either problem. The Collatz numbers are re-derived from a classical inequality, not new; Barina's 2025 bound needs a sharper averaging than this inequality. The Juggler 1-cycle length 9809721694 survives finance at N_0 = 3.5e8, which records that no Juggler analogue of Steiner's theorem exists, not that a cycle does.
