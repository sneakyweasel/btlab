# The even-run dual of Lemma 8

Status: **THE_EVEN_RUN_DUAL_IS_CAPPED_BELOW_LEMMA_EIGHT_BY_THE_SHAPE**

Generated by `python -m research.juggler_sequence.even_run_mersenne_floor`.

## The dual floor

On a cycle of the `Z` shortcut map with at least one even step, odd elements `x_1, ..., x_o` and `r_i` halvings between consecutive odd steps, let `R` be the gcd of the nonzero `r_i` and `m` any divisor of `2^R - 1` with `gcd(m, 2^o - 3^o) = 1`. Then `x = -1 (mod m)` for every odd element, hence `|x| >= m - 1`.

In `u = x + 1` the passage is `2^(r_i + 1) u_(i+1) = 3 u_i - 2 + 2^(r_i + 1)`; `ord_m(2) | R | r_i` makes `2^(r_i + 1) = 2 (mod m)`, the correction vanishes, and `2 u_(i+1) = 3 u_i`. Once around: `u (2^o - 3^o) = 0 (mod m)`.

Lemma 8 reads the **valuation** of `2` off the odd runs and gives `|x| >= 2^a - 1`; this reads the **order** of `2` off the even runs and gives `|x| >= 2^R - 1`.

## Verification

- block expansion `evenCharge w = sum_j 3^j 2^(A_j) (2^(r_j) - 1)`: `131070` words, `0` violations
- `(2^R - 1) | evenCharge w`: `5142` words with `R >= 2`, `0` violations
- the congruence at every odd element of the rational cycle: `3155` words, `0` violations
- vacuous at every known cycle: `True` (each has an E-run of length 1, so `R = 1`)

## The cap, and why it is the shape that imposes it

On a word whose proper prefixes are all non-contracting the leading run of `a` odd letters is followed by an E-run of length `r_1 >= 1`, and non-contraction at its end is `3^a >= 2^(a + r_1)`. Since `R | r_1`,

    R <= floor((log2 3 - 1) a) = floor(0.58496 a),

so the dual floor sits below Lemma 8's by `2^(0.41504 a)`.

| family | words | with `R >= 2` | max `R` | cap attained | exceptions | all circuits | dual beats Lemma 8 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| expanding | 198891 | 2209 | 8 | 612 | 0 | True | 0 |
| contracting | 12449 | 241 | 8 | 80 | 12 | True | 0 |

The exception class is exactly `O^a E^r, the circuit; Steiner 1977 excludes the nontrivial case`.

| leading odd run `a` | greatest `R` seen | Lemma 8 floor `2^a` |
| --- | --- | --- |
| 4 | 2 | 16 |
| 5 | 2 | 32 |
| 6 | 3 | 64 |
| 7 | 4 | 128 |
| 8 | 4 | 256 |
| 9 | 5 | 512 |
| 10 | 5 | 1024 |
| 11 | 6 | 2048 |
| 12 | 7 | 4096 |
| 13 | 7 | 8192 |
| 14 | 8 | 16384 |
| 15 | 7 | 32768 |
| 16 | 6 | 65536 |
| 17 | 5 | 131072 |
| 18 | 4 | 262144 |
| 19 | 3 | 524288 |
| 20 | 2 | 1048576 |

## Against the finance ceiling: zero new kills

On the `198891` CycleMin shape words of length at most `22`, sieved against `neg_cycle_finance`:

- Lemma 8 kills `168460`
- the dual kills `1009`, of which `1009` were already dead
- the dual kills alone: `0`

## What the reduction is

- mod 2^R - 1 with R dividing every even run, 2^(r_i + 1) = 2 and the correction 2^(r_i + 1) - 2 vanishes, leaving 2 u_(i+1) = 3 u_i
- which is the Juggler exponent transport e -> 3e/2 (odd base), e -> e/2 (even base) on the perfect-power locus, where there is no floor loss at all
- where the no-cycle argument is 3^o != 2^o, a pure exponent count needing neither Catalan nor Baker; the exponent map has no nontrivial cycle for that reason alone
- and the price is that pulled back through the congruence the equation 3^o != 2^o degrades from a contradiction into m | u -- an impossibility becomes a floor
- and it stops because the floor it becomes is capped by the CycleMin shape at 2^(floor((log2 3 - 1) a)), below Lemma 8's own 2^a

## Novelty

not established; arxiv.org, cs.uwaterloo.ca and oeis.org are refused by this container's network egress policy, so Hercher 2023, Eliahou 1993 and Simons-de Weger 2005 could not be read at source for the dual congruence.

## What this does not say

No Juggler cycle is excluded, no floor is raised, N_0 is untouched, and no negative Collatz cycle is newly excluded. The dual floor is a theorem about cycles of the Z shortcut map; its laboratory content is that the CycleMin shape caps it below the floor this laboratory already has, so it closes a direction rather than opening one. Neither map is claimed to halt.
