# Krasikov-Lagarias residue symmetry: the height transfer remains open

Generated by `python -m research.juggler_sequence.negative_preimage_density`.

Negation carries the formal residue program to the opposite sign. The assigned homogeneous shifts need a separate height argument for actual minus-map trees. The former density-transfer claim is withdrawn as unproved; its asymptotic conclusion is not refuted.

At target 19, child 13 and cutoff 103, the nominal child cutoff is `4017/38`. It admits the ancestor `104 -> 52 -> 26 -> 13`, above the actual cutoff. The child tree counts are 12 and 13. The exact scaling correction is `39/38` (Lean).

**Negation is an isomorphism at every k checked:** True (k = 2 to 10).

## Exponents from the solved system

| k | classes | plus model exponent | minus model exponent | agree |
|---|---|---|---|---|
| 2 | 3 | 0.4366 | 0.4366 | yes |
| 3 | 9 | 0.6113 | 0.6113 | yes |
| 4 | 27 | 0.6891 | 0.6891 | yes |
| 5 | 81 | 0.7336 | 0.7336 | yes |
| 6 | 243 | 0.7608 | 0.7608 | yes |

Published anchors: Krasikov 1989 reports 0.43 at k = 2, Krasikov-Lagarias 2003 reports 0.84 at k = 11.

Solved once at the k this probe is too slow for, both maps identical at each: k = 9 gives 0.8168, k = 10 gives 0.8295, k = 11 gives 0.8418. The last is the 0.84 they publish, from their own k, which is what says the system solved here is theirs.

## The underlying tree identity

`pi*_a(x) = 2 + pi*_4a(x) + pi*_c(x)` with `c = (2a+1)/3`, checked on 366 cases with 0 failures. The `3x+1` expression `(2a-1)/3` is nonintegral in all 366 cases. 10 fertile values were skipped as cycle members, and the identity fails on 10 of those, which is why the exclusion is a hypothesis and not a convenience.
