# The 3x-1 verification floor

Status: **NEGATIVE_FLOOR_MAKES_THE_MIRROR_A_STATEMENT**

Generated by `python -m research.juggler_sequence.negative_floor_3x1`.

## The statement

Every 1 <= y < 2^51 reaches 1, 5 or 17 (CPU certificate to 2^44, GPU sweep to 2^51). Hence, by neg_cycle_finance (kernel-checked), a fourth cycle of the 3x-1 shortcut map -- equivalently a fourth negative cycle of shortcut 3x+1, whose word is a Paper A CycleMin shape -- has period at least 85137581, with 53715833 odd steps.

## Certificate

- chunks: `112`, covering `[3, 17592186044416)` (`= 2^44`: `True`; contiguous: `False`)
- odd starts between chunks, each verified by full iteration: `[34359738369, 68719476737, 103079215105, 137438953473, 171798691841, 206158430209, 240518168577]` (all reach a known cycle: `True`)
- chunks by verifier: `{'verify_3x1.c': 16, 'verify_3x1_jump.c': 96}`
- odd starts: `8796093022184`
- failures: `0`; new cycles: `0`
- greatest step count by verifier: `{'verify_3x1.c': 544, 'verify_3x1_jump.c': 704}` against caps `{'verify_3x1.c': 4000, 'verify_3x1_jump.c': 40000}`
- greatest excursion: `121443575752945981388885320`, state `unsigned __int128` (above `2^40` a maximum over walked starts and jump landings only)
- reference agreement (descent against full iteration): `True` to `60000`

## GPU extension

- `[17592186044416, 2251799813685248)` = `[2^44, 2^51)` by `verify_3x1_gpu.cu` on NVIDIA GeForce RTX 5090, 595.79: 8 chunks, odd starts `1117103813820416` (coverage exact: `True`), failures `0`, new cycles `0`, overflows `0`, greatest step count `847`, peak `154710878312175791134940888500` (about 2^97.0), 3517.1 s wall
- calibration on the CPU's range agrees: `True`; spot checks against the CPU jump walker inside the new range agree: `True`
- combined floor: `2^51`

## What each floor buys

| floor | least period | odd steps | floor verified here |
| --- | --- | --- | --- |
| `2^38` | 4404167 | 2778720 | True |
| `10^11` | 1988215 | 1254424 | True |
| `2^40` | 9538065 | 6017849 | True |
| `2^44` | 16483927 | 10400200 | True |
| `2^51` | 85137581 | 53715833 | True |
| `2^68` | (beyond the walk cap) | -- | False |

## What this does not say

The floor is a computation, not a theorem, and it is this laboratory's computation rather than a literature import: the recorded search found no published 3x-1 floor, which is not the same as none existing. N_0 = 350000000 is the Juggler's own cycle floor and is untouched; this is a different object for a different map. No Juggler cycle is excluded, no Juggler bound moves, Paper A is unchanged, and neither map is claimed to halt. The implication from floor to period is kernel-checked (neg_cycle_finance); only the floor is empirical.
