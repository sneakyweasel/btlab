# Hercher's Lemma 8 on the exponent

Status: **LEMMA_EIGHT_MIRROR_IS_THE_EXPONENT_VALUATION**

Generated by `python -m research.juggler_sequence.exponent_valuation_mirror`.

## The dictionary

| | Collatz | Juggler |
| --- | --- | --- |
| carrier of the 2-adic integer | the value `u = x + 1` | the exponent `e(n)` |
| the map | `u -> 3u/2` | `e -> 3e/2` (odd), `e -> e/2` (even) |
| why one unit per step | `3` is a 2-adic unit | the same |
| run law | `run(x) = v_2(x+1)` | `exactRun(n) = v_2(e(n))` |
| pointwise floor | `x >= 2^k - 1` | `n >= 2^(2^k)`, odd `n >= 3^(2^k)` |
| count at depth `k` | `~ 2^(-k) N` | `~ N^(2^-k)` |
| word on the fibre | bijection onto `2^k` words (Terras) | the `2` monochrome words |

## Run law

- `exactRun(n) = v_2(e(n))` on `2 <= n <= 1000000`: `True` (999 square states, 19859 non-square states)
- `run(x) = v_2(x+1)` on 100000 odd starts: `True`
- exact runs monochrome: `True`

## Counting

| k | Juggler measured | `floor(N^(2^-k)) - 1` | Collatz measured | `floor((N+1)/2^k)` |
| --- | --- | --- | --- | --- |
| 0 | 999999 | 999999 | 1000000 | 1000000 |
| 1 | 999 | 999 | 500000 | 500000 |
| 2 | 30 | 30 | 250000 | 250000 |
| 3 | 4 | 4 | 125000 | 125000 |
| 4 | 1 | 1 | 62500 | 62500 |
| 5 | 0 | 0 | 31250 | 31250 |

`N = 1000000`, every row exact: `True`.

## Minimal realizers

| k | Collatz `2^k - 1` | Juggler `log2` of the minimum | odd Juggler `log3` | word |
| --- | --- | --- | --- | --- |
| 0 | 0 | 1 | 1 | `(empty)` |
| 1 | 1 | 2 | 2 | `O` |
| 2 | 3 | 4 | 4 | `OO` |
| 3 | 7 | 8 | 8 | `OOO` |
| 4 | 15 | 16 | 16 | `OOOO` |
| 5 | 31 | 32 | 32 | `OOOOO` |

All attained: `True`. log(3^(2^k)) = (2^k) log 3, so the Juggler floor is the exponential of the Collatz floor -- the walk dictionary, pointwise

## The fibre

- n = p^e has v_p(n) = e; the Juggler sends v_p to 3 v_p / 2 on an odd prime and to v_p / 2 at p = 2
- the even step sends v_2(x) to v_2(x) - 1; the odd step sends u = x + 1 to 3u/2
- the Juggler does to valuations what Collatz does to values; checked on primes `[2, 3, 5, 7, 11]` to exponent `40`: `True`

## The hand-over

- e odd: floorPower(2^e) = floor(2^((e-1)/2) sqrt 2), parity = bit_{(e-1)/2}(sqrt 2)
- checked on 201 odd exponents up to `401`: `True`
- first 32 bits of `sqrt 2`: `10110101000001001111001100110011`
- n = m^e with e odd: the first inexact image is floor(m^((3e-1)/2) sqrt m) on an odd base and floor(m^((e-1)/2) sqrt m) on an even one

## Fibre entropy

| length | Collatz words per residue system | bijective | Juggler words on the locus |
| --- | --- | --- | --- |
| 1 | 2 | True | 2 |
| 2 | 4 | True | 2 |
| 3 | 8 | True | 2 |
| 4 | 16 | True | 2 |
| 5 | 32 | True | 2 |
| 6 | 64 | True | 2 |
| 7 | 128 | True | 2 |
| 8 | 256 | True | 2 |

## What this does not say

No floor is raised, no cycle of any length is excluded in either problem, and no Collatz bound is improved; Lemma 8 is quoted from hercher-2023-collatz-m-cycles, not reproved. The mirror's hypothesis is k consecutive EXACT steps, a condition on the integer, where Hercher's is k consecutive odd LETTERS, a condition on the word; no word implies the former, so the mirror is inert for cycles and does not weaken the 19 September negative-knowledge entry. The pointwise odd-run bound for inexact runs -- the odd-tower fragment -- stays open and stays Archimedean.
