import { GapTransferExplorer } from "../../components/GapTransferExplorer";

export function GapTransferTab() {
  return (
    <div className="space-y-5">
      <p className="prose-measure text-sm text-muted">
        Contribution 5 of the paper in three pictures. The surplus becomes a
        linear form; Rhin’s measure then excludes only the short{" "}
        <span className="font-mono">(n, L)</span> pairs. Move the cycle
        minimum and the period. The named survivors sit in the long regime.
        The site does not claim that Rhin kills a finance survivor, and it
        does not reopen Baker as a leftover killer.
      </p>
      <GapTransferExplorer />
    </div>
  );
}
